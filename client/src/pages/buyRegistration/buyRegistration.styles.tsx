import styled from 'styled-components';

export const BuyRegistrationWrapper = styled.div`
  background: ${(props) => props.theme.modalBackground};
  border-radius: 5px;
  padding: 20px;
  margin: auto;
`;
