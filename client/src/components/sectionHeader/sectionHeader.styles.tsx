import styled from 'styled-components';

export const SectionHeaderH3 = styled.h3`
  font-family: ${props => props.theme.font};
  color: ${props => props.theme.mainTextColor};
  margin: 10px 0;
  padding: 0 10px;
  font-size: 16px;
`;
